type Query {
  events: [Event!]!
}

type Event {
  id: ID!
  name: String!
}

type Feedback {
  id: ID!
  eventId: ID!
  userName: String!
  rating: Int!
  description: String!
}

query GetEvents {
  events {
    id
    name
  }
}
